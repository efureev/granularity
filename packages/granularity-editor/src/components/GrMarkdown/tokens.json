{
  "tokens": [
    {
      "name": "--gr-markdown-measure",
      "kind": "hook",
      "default": "68ch",
      "description": "Мера строки: длинная строка читается плохо. Ограничение получают текстовые блоки, а таблица, код и картинка выходят за неё во всю ширину."
    },
    {
      "name": "--gr-markdown-flow",
      "kind": "hook",
      "default": "1em",
      "description": "Вертикальный ритм между блоками. В `em`, а не в пикселях: интервал обязан идти за кеглем."
    },
    {
      "name": "--gr-markdown-heading-flow",
      "kind": "hook",
      "default": "1.8em",
      "description": "Отбивка перед заголовком. Больше обычного ритма: заголовок принадлежит тому, что под ним."
    },
    {
      "name": "--gr-markdown-link",
      "kind": "hook",
      "default": "var(--gr-primary-text)",
      "description": "Цвет ссылки в тексте."
    },
    {
      "name": "--gr-markdown-code-bg",
      "kind": "hook",
      "default": "var(--gr-muted)",
      "description": "Подложка кода в строке."
    },
    {
      "name": "--gr-markdown-pre-bg",
      "kind": "hook",
      "default": "var(--gr-muted)",
      "description": "Фон блока кода. Чтобы он совпал с `GrCodeBlock` из `granularity-code`, потребитель ставит `--gr-markdown-pre-bg: var(--gr-code-block-bg)`: ссылаться на чужой токен из своего CSS пакет не вправе — его провайдера здесь нет."
    },
    {
      "name": "--gr-markdown-quote-bar",
      "kind": "hook",
      "default": "var(--gr-brd)",
      "description": "Полоса слева от цитаты."
    },
    {
      "name": "--gr-markdown-rule",
      "kind": "hook",
      "default": "var(--gr-brd)",
      "description": "Линия разделителя и границы таблицы."
    },
    {
      "name": "--gr-markdown-table-header-bg",
      "kind": "hook",
      "default": "var(--gr-muted)",
      "description": "Подложка шапки таблицы."
    },
    {
      "name": "--gr-markdown-block-size",
      "kind": "inline",
      "default": "3rem",
      "description": "Оценка высоты блока для `contain-intrinsic-size`. Ставится рендерером на каждый блок: без неё полоса прокрутки скачет, потому что нерасмеченный блок считается нулевой высоты."
    }
  ]
}
