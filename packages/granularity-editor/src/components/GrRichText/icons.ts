/**
 * Иконки тулбара — контуром внутри пакета, а не классами `i-lucide-*`.
 *
 * Класс иконки генерирует не пакет, а UnoCSS-сборка приложения: у потребителя
 * без своего `presetIcons` на месте картинки осталась бы пустота. Тем же
 * приёмом рисуют иконки пикеры `granularity-chrono`.
 *
 * Все пути — в сетке 24×24, штрихом `currentColor`: цвет и толщина приходят от
 * кнопки, а не задаются здесь.
 */

/** Путь иконки по ключу действия. `d` — содержимое одного или нескольких `path`. */
export const actionIconPaths: Record<string, string[]> = {
  // Полукруглые чаши буквы «B».
  bold: [
    'M7 5h6a3.5 3.5 0 0 1 0 7H7z',
    'M7 12h7a3.5 3.5 0 0 1 0 7H7z',
  ],
  // Наклонная с засечками — «I».
  italic: [
    'M15 5h-5M14 19H9M14 5l-4 14',
  ],
  strike: [
    'M4 12h16',
    'M8 7a3 3 0 0 1 3-2h3a3 3 0 0 1 3 3M16 17a3 3 0 0 1-3 2h-3a3 3 0 0 1-3-3',
  ],
  // Угловые скобки: код внутри строки.
  code: [
    'm9 9-3 3 3 3M15 9l3 3-3 3',
  ],
  bulletList: [
    'M9 6h11M9 12h11M9 18h11',
    'M4.5 6h.01M4.5 12h.01M4.5 18h.01',
  ],
  orderedList: [
    'M10 6h10M10 12h10M10 18h10',
    'M4 6h1v4M4 10h2',
    'M4 15.5A1.5 1.5 0 1 1 6 17l-2 2h2.5',
  ],
  // «H» с цифрой уровня: два заголовка обязаны отличаться на глаз.
  heading2: [
    'M4 6v12M11 6v12M4 12h7',
    'M15.5 9.5a2 2 0 1 1 3.5 1.4L15.5 18H20',
  ],
  heading3: [
    'M4 6v12M11 6v12M4 12h7',
    'M15.5 9a2 2 0 1 1 2.2 3H17h.7a2 2 0 1 1-2.2 3',
  ],
  blockquote: [
    'M6 5v14',
    'M11 8h8M11 12h8M11 16h5',
  ],
  // Скобки на отдельной подложке — блок, а не строка.
  codeBlock: [
    'M4 5h16v14H4z',
    'm10 10-2 2 2 2M14 10l2 2-2 2',
  ],
}

/** Есть ли у действия своя иконка. Кнопка без неё рисует подпись словом. */
export function iconPathsFor(key: string): string[] {
  return actionIconPaths[key] ?? []
}
