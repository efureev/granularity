import process from 'node:process'

/**
 * Часовой пояс прогона зафиксирован намеренно.
 *
 * Пакет существует ради того, чтобы арифметика дат не зависела от перевода
 * часов, и проверить это можно только в зоне, где перевод есть. Без явного
 * `TZ` тесты давали бы разный результат на машине разработчика и в CI —
 * то есть ровно ту невоспроизводимость, от которой лечим.
 *
 * `America/New_York`: летнее время в 2026-м начинается 8 марта и кончается
 * 1 ноября — обе границы попадают в тесты.
 */
process.env.TZ = 'America/New_York'
