/**
 * Оформления у метки нет намеренно — та же причина, что у `GrRelativeTime`:
 * длительность стоит внутри строки текста и обязана выглядеть как она.
 * Единственный класс держит то, чего наследование не даст: запрет переноса
 * посреди «2 ч 30 мин».
 */
export const durationClass = 'whitespace-nowrap'
