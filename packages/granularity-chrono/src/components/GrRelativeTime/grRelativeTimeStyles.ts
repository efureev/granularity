/**
 * Оформления у метки нет намеренно: она обязана выглядеть как текст вокруг
 * неё — кегль, цвет и начертание наследуются от контекста. Свой класс здесь
 * ровно один и держит единственное, чего наследование не даст: запрет переноса
 * внутри «3 минуты назад», иначе строка рвётся посреди значения.
 */
export const relativeTimeClass = 'whitespace-nowrap'
