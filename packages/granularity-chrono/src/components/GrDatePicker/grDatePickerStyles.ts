import type { GrPickerSize } from '../../internal/pickerFieldStyles'

/**
 * Публичное имя типа размера компонента. Сами классы поля общие для всех
 * пикеров пакета и живут в `internal/pickerFieldStyles`.
 */
export type GrDatePickerSize = GrPickerSize
