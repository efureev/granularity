# GrModal

Примитив модального слоя: панель, бэкдроп, слой, скролл-лок, фокус-ловушка.
Заголовков, подвалов и кнопки закрытия здесь нет намеренно — это
[`GrDialog`](../components.md), который строится поверх. `GrModal` берут, когда
содержимое окна не укладывается в «шапка — тело — подвал».

Слои и порядок Esc — [`../z-index.md`](../z-index.md), клавиатура —
[`../keyboard.md`](../keyboard.md).

## Имя окна обязательно

Модальный слой без доступного имени — нарушение, которое диктор озвучивает как
безымянный «диалог», а axe ловит правилом `aria-dialog-name`. HeadlessUI
связывает `aria-labelledby` только при наличии `DialogTitle`, то есть при слоте
`#title`. Поэтому:

1. есть `#title` — имя даёт он;
2. нет `#title`, но есть `ariaLabel` — имя берётся из пропа;
3. нет ничего — подставляется обобщённое имя из локали (`gr.modal.title`), а в
   dev-сборке при первом открытии печатается предупреждение.

Третий пункт — страховка, а не режим работы: обобщённое «Диалог» лучше пустоты,
но осмысленное имя знает только автор окна.

```vue
<GrModal v-model="open" aria-label="Импорт из CSV">
  <ImportWizard />
</GrModal>
```

## Скролл длинного содержимого

`scrollBehavior`:

- `outside` (по умолчанию) — скроллится весь оверлей, окно уезжает вверх
  вместе со страницей;
- `inside` — панель ограничена высотой вьюпорта, а скроллится только её тело.
  Слоты `#title` и `#description` при этом остаются на месте: панель становится
  колонкой, и заголовок не уезжает вместе с содержимым.

Скролл всегда ровно в одном месте: два скроллбара на одно окно — это баг, а не
запас прочности.

Оговорка для `GrDialog`: он отдаёт `GrModal` шапку, тело и подвал одним куском
через default-слот, поэтому при `inside` они скроллятся вместе. Закрепить шапку
диалога — отдельная задача уровня `GrDialog`, не примитива.

## Esc, слой и фокус

Окно регистрируется в общем стеке слоёв (`useOverlayLayer`, `modal: true`).
Стек гасит Escape в capture-фазе на `window`, поэтому:

- закрывается **верхний** слой, а не тот, что оказался ниже по документу:
  дропдаун, открытый внутри окна, по Esc закрывает себя, а следующий Esc — уже
  окно;
- до `<Dialog>` HeadlessUI нажатие не доходит вовсе. Значит `@close` от него
  остаётся только у клика по бэкдропу и программного вызова — оба подчиняются
  `closeOnBackdrop`, и `closeOnEsc` с ним не пересекается.

Нижние открытые окна помечаются `inert`, чтобы фокус-ловушка нижнего не
отбирала фокус у верхнего. Диалоги `useDialogService` монтируются отдельным
`render()` в `body` и всё равно попадают в тот же стек — в этом его смысл.

`initialFocus` задаёт элемент, получающий фокус при открытии. По умолчанию это
сама панель (`tabindex="-1"`): она фокусируема программно, и HeadlessUI не
жалуется на окно без фокусируемого содержимого.

## Жизненный цикл

`opened` и `closed` эмитятся **после** анимации. `closed` — единственный
безопасный момент, чтобы размонтировать содержимое: сделать это по
`update:modelValue` значит оборвать анимацию закрытия на полпути.
