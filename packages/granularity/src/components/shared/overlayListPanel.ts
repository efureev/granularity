/**
 * Панель списка, ужимающаяся до места, оставшегося до края вьюпорта.
 *
 * Слой позиционирования публикует замер в `--gr-floating-available-height`
 * (`composables/useFloating.ts`), и панель берёт его потолком: собственный
 * `dropdownMaxHeight` на коротком экране больше доступного места, а сжать
 * панель не умеют ни `flip`, ни `shift` — низ списка просто уходит за край.
 *
 * Колонка обязательна вместе с потолком. Панель держит не только список, но и
 * поле поиска, строку «добавить своё» и строки состояний; без `flex-col` и
 * `min-h-0` у списка ужимался бы не он, а панель целиком — то есть вместо
 * одного скролла вышло бы два.
 *
 * Модуль отдельный от `overlayPanelSurface` намеренно: ту поверхность берёт и
 * `GrPopover` со своим потолком, а класс из общего модуля обязан объявить в
 * safelist **каждый** импортёр, даже не пользующийся им.
 */
export const overlayListPanelClass = 'flex flex-col max-h-[var(--gr-floating-available-height,100vh)]'
