/**
 * У группы своих вариантов нет: она раскладывает чипы и ничего не красит.
 * Класс вынесен сюда, а не оставлен литералом в шаблоне, только ради safelist —
 * из `.vue` литералы попадают в скан сами, но тогда список классов группы жил
 * бы в двух местах.
 */
export const chipGroupRootClass = 'flex flex-wrap items-center gap-2'
