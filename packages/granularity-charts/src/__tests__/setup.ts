import process from 'node:process'

/**
 * Зона с переводом часов: лестница делений оси времени (`chartTicks.timeTicks`)
 * шагает месяцы и годы конструктором `Date`, и переход на летнее время — ровно
 * тот случай, где фиксированный шаг в миллисекундах даёт дубль деления либо
 * теряет его. В UTC этот класс ошибок не воспроизводится вовсе.
 */
process.env.TZ = 'America/New_York'
