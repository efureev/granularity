{
  "owner": "GrChartHeatmap",
  "tokens": [
    {
      "name": "--gr-chart-heatmap-empty",
      "kind": "hook",
      "default": "transparent",
      "description": "Заливка отсутствующей ячейки. Прозрачная намеренно: «ячейки нет» и «значение ноль» обязаны выглядеть по-разному."
    },
    {
      "name": "--gr-chart-heatmap-outline",
      "kind": "hook",
      "default": "var(--gr-fg)",
      "description": "Обводка активной ячейки. Один цвет на всю карту: цветная рамка спорила бы с заливкой."
    },
    {
      "name": "--gr-chart-heatmap-label",
      "kind": "hook",
      "default": "var(--gr-fg)",
      "description": "Подпись значения в ячейке на светлой заливке. Задаётся атрибутом `fill`: SVG не красится `color`."
    }
  ]
}
