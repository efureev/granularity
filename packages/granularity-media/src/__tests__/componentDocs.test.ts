import { defineComponentDocsGate } from '@feugene/granularity-test-kit/gates'

/**
 * Гейт на страницу компонента: без неё компонент нельзя выбрать, не читая его
 * исходник.
 */
defineComponentDocsGate()
