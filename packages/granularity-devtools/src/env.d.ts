/**
 * Версия пакета, подставляемая на сборке (`define` в `vite.config.ts`).
 *
 * Нужна мосту: тест, снявший снимок, обязан понимать, с какой версией панели
 * он разговаривает. В рантайме такого имени нет — к потребителю уезжает уже
 * подставленный литерал.
 */
declare const __GR_DEVTOOLS_VERSION__: string
