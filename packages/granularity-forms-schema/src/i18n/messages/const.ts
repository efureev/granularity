/**
 * Блок переводов пакета.
 *
 * Свой, а не `gr`: блоки ядра и спутника сливаются в один словарь, и общий
 * ключ означал бы, что одна строка молча затирает другую.
 */
export const GR_FORMS_SCHEMA_I18N_BLOCK = 'grForms'
