/**
 * Иконки кнопки «скопировать» — контуром внутри пакета, а не классами `i-lucide-*`.
 *
 * Класс иконки генерирует не пакет, а UnoCSS-сборка приложения: у потребителя
 * без своего `presetIcons` на месте картинки осталась бы пустота. В ядре блок
 * жил с `~icons/*`, потому что там `presetIcons` есть у витрины; спутник такого
 * допущения делать не вправе. Тем же приёмом рисуют иконки `granularity-editor`
 * и пикеры `granularity-chrono`.
 *
 * Сетка 24×24, штрих `currentColor`: цвет и толщина приходят от кнопки.
 */
export const copyIconPaths: string[] = [
  'M16 4h2a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2v-2',
  'M14 2H6a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2z',
]

export const checkIconPaths: string[] = [
  'M20 6 9 17l-5-5',
]
