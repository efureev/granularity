import { defineGateCoverage } from '@feugene/granularity-test-kit/gates'

/**
 * Список обязательных гейтов задан явно и короче общего: семь остальных фабрик
 * проверяют компоненты — реестр, покомпонентные токены, страницы, имена
 * событий, — а компонентов у пакета нет. Умолчание требовало бы их все, и
 * пришлось бы либо заводить пустышки, либо отключать гейт целиком.
 */
defineGateCoverage({ required: ['defineEnvGuardGate'] })
