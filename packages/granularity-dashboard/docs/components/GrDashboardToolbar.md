# GrDashboardToolbar

Переключатель режима дашборда и сброс раскладки. Готовая полоса над сеткой —
чтобы «редактировать» и «вернуть как было» не собирались заново в каждом
приложении и не расходились между экранами.

Режимы и раскладка — [`../model.md`](../model.md), клавиши —
[`../keyboard.md`](../keyboard.md), роли и объявления —
[`../a11y.md`](../a11y.md), токены — [`../theming.md`](../theming.md).

```vue
<GrDashboardToolbar v-model:mode="mode" resettable @reset="reset" />
```

## Когда брать

- **дашборд редактируемый** — пользователю нужен явный вход в режим, иначе
  ручки переноса либо висят всегда, либо не находятся вовсе;
- **раскладку можно вернуть** — `resettable` даёт сброс к исходной; без него
  испорченную раскладку нечем починить, кроме как чистить хранилище;
- **рядом нужны свои кнопки** — слоты `#start`, `#end` и содержимое по
  умолчанию принимают «Добавить виджет», период, фильтр;
- **режим хранится снаружи** — `v-model:mode` держит родитель, поэтому в него
  можно войти и из другого места экрана.

## Когда взять другое

| Нужно | Берите |
| --- | --- |
| Просто ряд кнопок над контентом | [`GrButtonGroup`](../../../granularity/docs/components/GrButtonGroup.md) |
| Переключатель двух-трёх режимов сам по себе | [`GrSegmented`](../../../granularity/docs/components/GrSegmented.md) |
| Верхняя панель приложения | [`GrNavbar`](../../../granularity/docs/components/GrNavbar.md) |
| Каталог виджетов для добавления | [`GrDashboardPalette`](./GrDashboardPalette.md) |
| Сама сетка | [`GrDashboard`](./GrDashboard.md) |

## Сброс подтверждения не спрашивает

Компонент излучает намерение, а применяет его потребитель: только он знает,
сохранена ли раскладка на сервере и стоит ли спросить. Диалог внутри тулбара
означал бы, что пакет решил за приложение, насколько разрушительна операция.

Спросить нужно — рядом ставят
[`GrConfirmDialog`](../../../granularity/docs/components/GrConfirmDialog.md) или
`useDialogService().confirm()`.
