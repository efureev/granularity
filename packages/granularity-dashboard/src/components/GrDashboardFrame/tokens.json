{
  "owner": "GrDashboardFrame",
  "tokens": [
    {
      "name": "--gr-dashboard-frame-placeholder-bg",
      "kind": "hook",
      "default": "var(--gr-muted)",
      "description": "Заливка подложки — места, куда встанет переносимый виджет. Обязана быть тише самих виджетов: подложка показывает намерение, а не содержимое."
    },
    {
      "name": "--gr-dashboard-frame-placeholder-brd",
      "kind": "hook",
      "default": "var(--gr-primary)",
      "description": "Пунктирная рамка подложки. Единственное, что отличает её от пустого места на плотной сетке."
    },
    {
      "name": "--gr-dashboard-frame-placeholder-radius",
      "kind": "hook",
      "default": "var(--gr-radius-lg)",
      "description": "Скругление подложки. Совпадает со скруглением виджета, иначе место читается как чужое."
    },
    {
      "name": "--gr-dashboard-frame-placeholder-inset",
      "kind": "hook",
      "default": "4px",
      "description": "Отступ подложки внутрь своей ячейки. Нулевой отступ кладёт пунктир ровно на границу соседа, и там, где стороны совпадают, рамка выглядывает из-под чужой карточки."
    },
    {
      "name": "--gr-dashboard-frame-handle-color",
      "kind": "hook",
      "default": "var(--gr-muted-fg)",
      "description": "Цвет ручек переноса и растягивания в покое."
    },
    {
      "name": "--gr-dashboard-frame-handle-size",
      "kind": "hook",
      "default": "1.5rem",
      "description": "Сторона ручки переноса. Ниже 24px цель становится непопадаемой пальцем."
    },
    {
      "name": "--gr-dashboard-frame-resize-size",
      "kind": "hook",
      "default": "14px",
      "description": "Сторона уголка растягивания в правом нижнем углу виджета."
    },
    {
      "name": "--gr-dashboard-frame-resize-radius",
      "kind": "hook",
      "default": "var(--gr-radius-lg)",
      "description": "Скругление уголка растягивания. Совпадает со скруглением виджета: прямой угол на скруглённой карточке торчит за её край. Переопределив `--gr-dashboard-item-radius`, переопределите и его."
    },
    {
      "name": "--gr-dashboard-frame-ghost-bg",
      "kind": "hook",
      "default": "var(--gr-card)",
      "description": "Фон призрака — того, что видно под курсором между каталогом и сеткой. Плотный, а не полупрозрачный: он проезжает над произвольным содержимым страницы."
    },
    {
      "name": "--gr-dashboard-frame-ghost-brd",
      "kind": "hook",
      "default": "var(--gr-primary)",
      "description": "Рамка призрака. Тот же цвет, что и у подложки в сетке: несомое и место, куда оно встанет, — одно намерение."
    },
    {
      "name": "--gr-dashboard-frame-ghost-radius",
      "kind": "hook",
      "default": "var(--gr-radius-lg)",
      "description": "Скругление призрака. Совпадает со скруглением виджета и подложки."
    },
    {
      "name": "--gr-dashboard-frame-ghost-shadow",
      "kind": "hook",
      "default": "var(--gr-shadow-3)",
      "description": "Тень призрака: он поднят над страницей так же, как поднятый виджет над сеткой."
    },
    {
      "name": "--gr-dashboard-frame-ghost-z",
      "kind": "hook",
      "default": "var(--gr-z-toast)",
      "description": "Слой призрака. Выше окна: перенос начинают и из модалки, а призрак живёт ровно пока палец на кнопке."
    }
  ]
}
