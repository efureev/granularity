{
  "examples": {
    "table-basic-rows": {
      "title": "Basic row rendering",
      "description": "For the base page we show canonical table markup: the `#header` slot, body rows and composition with badges."
    },
    "table-loading-state": {
      "title": "Loading rows with skeletons",
      "description": "Covers the data-display edge case: the table should look predictable in a loading state, when data has not arrived yet."
    },
    "table-empty-state": {
      "title": "Empty state inside tbody",
      "description": "Shows how `GrTable` can host a `GrEmptyState` inside `tbody` without losing table semantics and the visual shell."
    }
  }
}
