{
  "examples": {
    "dialog-basic-flow": {
      "title": "Basic dialog shell",
      "description": "Shows the base layer over `GrModal`: a ready-made header/footer shell for review, approval and confirm-like scenarios."
    },
    "dialog-section-config": {
      "title": "Section config and internal state",
      "description": "Demonstrates `headerConfig` / `footerConfig` and local form state inside the dialog shell."
    },
    "dialog-guarded-backdrop": {
      "title": "Guarded backdrop for critical flows",
      "description": "A dedicated scenario for `closeOnBackdrop=false`, when closing must happen only through explicit actions."
    }
  }
}
