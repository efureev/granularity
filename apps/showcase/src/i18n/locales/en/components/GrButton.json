{
  "examples": {
    "button-builder": {
      "title": "Interactive button constructor",
      "description": "A live playground for all key `GrButton` props: change `variant`, `tone`, size, type and states without switching between separate demo cards.",
      "note": "The best format for design review and QA: one scenario covers all component props at once and helps quickly verify accessibility of the icon-only mode."
    },
    "button-state-matrix": {
      "title": "Tone × variant state matrix",
      "description": "A full matrix across every `tone` and `variant`, including live, `hover`, `focus` and `active`, for design review and visual regression.",
      "note": "This is the same scenario that used to live in `playground-5`: it is handy for comparing new tones and checking the state contract without manual hovering."
    }
  }
}
