{
  "examples": {
    "switch-builder": {
      "title": "Interactive switch constructor",
      "description": "Assemble `GrSwitch` for your scenario: change the state, size, label and local color overrides and get the resulting snippet instantly."
    },
    "switch-size-scale": {
      "title": "Size scale from compact to prominent",
      "description": "A single scenario shows how the switch scales from compact control bars to large form sections without changing behavior."
    },
    "switch-disabled-labeled": {
      "title": "Labeled switches and disabled state",
      "description": "Shows that the label lives in the default slot, and the disabled mode works equally well for a controlled and a statically enabled switch."
    },
    "switch-custom-colors": {
      "title": "Custom active and inactive colors",
      "description": "Captures one of the component's key integration capabilities: locally overriding track colors without changing the global theme."
    }
  }
}
